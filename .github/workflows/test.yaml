name: empty permission

on:
  pull_request:
    branches: main
# permissions: {}
permissions:
  contents: none
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: cat README.md
